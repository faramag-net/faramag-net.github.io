/**
 * Productos Santa Rosa
 * Módulo: Mercado
 * Versión: 1.1.3
 * Build: 20260926.1235
 * Objetivo: Exploración y alta de productos, presentaciones, empresas, precios y fotos.
 */
import {getProducts,getPresentations,productById,presentationsForProduct,placeById,getPlaces,getObservations,observationsForProduct,purchasesForProduct,createProduct,updateProduct,deactivateProduct,deleteProduct,createPresentation,updatePresentation,deactivatePresentation,deletePresentation,upsertPlace,deactivatePlace,deletePlace,addObservation,deleteObservation,addPurchase,savePhoto,getPhoto,deletePhoto,latestPriceForPresentationPlace,pricesForPresentation,normalize,uid} from './mercado-store.js';
const root=document.getElementById('app');let query='';let placeQuery='';let typeFilter='';let selectedProductId=null;let selectedPlaceId=null;let showPlaces=false;
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const dateFmt=d=>new Date(d).toLocaleDateString('es-MX',{day:'2-digit',month:'2-digit',year:'numeric'});
const ageDays=d=>Math.floor((Date.now()-new Date(d).getTime())/86400000);
const nav=active=>`<nav class="app-navbar"><div class="app-brand">🍓 Santa Rosa</div><a class="app-navlink ${active==='mercado'?'active':''}" href="/productos-santa-rosa/pages/mercado/">🏪 Mercado</a><a class="app-navlink ${active==='compras'?'active':''}" href="/productos-santa-rosa/pages/insumos/">📦 Compras</a><a class="app-navlink ${active==='mapa'?'active':''}" href="/productos-santa-rosa/pages/mapa/">🗺️ Mapa</a></nav>`;
function shell(content,active='mercado'){root.innerHTML=nav(active)+content;bind();}
function filteredProducts(){return getProducts().filter(p=>!query||normalize(p.nombre).includes(normalize(query))).filter(p=>!typeFilter||normalize(p.categoria)===normalize(typeFilter));}
function render(){if(selectedPlaceId){renderPlace();return;}if(selectedProductId){renderProduct();return;}if(showPlaces){renderPlaces();return;}const types=[...new Set(getProducts().map(p=>p.categoria).filter(Boolean))];const ps=filteredProducts();shell(`<div class="app-head"><div><h1>Mercado</h1><div class="muted">Explora y registra lo que encuentras.</div></div><div class="app-actions"><button class="btn" id="newProduct">+ Producto</button><button class="btn secondary" id="placesView">🏪 Empresas</button><button class="btn secondary" id="newPlace">+ Empresa</button></div></div><div class="search-row"><input id="q" class="search" value="${esc(query)}" placeholder="🔎 Buscar producto..."><button class="btn secondary filter-btn" id="filterBtn">☰ Filtros</button></div><div id="filters" class="filter-panel"><button class="filter-chip ${!typeFilter?'active':''}" data-type="">Todos</button>${types.map(t=>`<button class="filter-chip ${normalize(typeFilter)===normalize(t)?'active':''}" data-type="${esc(t)}">${esc(t)}</button>`).join('')}</div><div class="grid">${ps.map(p=>`<button class="tile product-tile" data-product-id="${p.id}"><span class="tile-icon">🛒</span><span class="tile-name">${esc(p.nombre)}</span><span class="tile-meta">${presentationsForProduct(p.id).length} presentaciones</span></button>`).join('')}</div>${!ps.length?'<div class="empty">No hay productos. Usa + Producto para dar de alta uno.</div>':''}<div class="section"><button class="section-title" data-section="summary">📊 Resumen <span>▼</span></button><div class="section-body" id="summary"><div class="stats"><span class="stat">${getProducts().length} productos</span><span class="stat">${getPresentations().length} presentaciones</span><span class="stat">${getPlaces().filter(p=>p.estatus!=='inactivo').length} empresas activas</span></div></div></div><div class="section"><button class="section-title" data-section="history">📋 Información reciente <span>▼</span></button><div class="section-body" id="history"><div class="list">${getObservations().sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt)).slice(0,12).map(o=>`<div class="row-card"><div class="row-main"><div><b>${esc(o.producto)}</b><div class="muted">${esc(o.presentacion||'Sin presentación')} · ${esc(placeById(o.clienteId)?.nombre||'Empresa')}</div></div><span class="price">$${Number(o.precio||0).toFixed(2)}</span></div><div class="hint">${dateFmt(o.createdAt)}</div></div>`).join('')||'<div class="empty">No hay precios registrados.</div>'}</div></div></div><div class="modal" id="modal"></div>`);}
function renderPlaces(){const all=getPlaces().filter(p=>p.estatus!=='inactivo');const list=all.filter(p=>!placeQuery||normalize(p.nombre).includes(normalize(placeQuery))||normalize(p.tipo).includes(normalize(placeQuery)));shell(`<div class="app-head"><div><button class="btn secondary small" id="backPlaces">← Mercado</button><h1>Empresas</h1><div class="muted">Catálogo de tiendas y lugares de abastecimiento.</div></div><div class="app-actions"><button class="btn" id="newPlaceFromList">+ Empresa</button></div></div><div class="search-row"><input id="placeQ" class="search" value="${esc(placeQuery)}" placeholder="🔎 Buscar empresa..."></div><div class="company-grid">${list.map(p=>`<button class="company-card" data-place-id="${p.id}"><b>🏪 ${esc(p.nombre)}</b><span>${esc(p.tipo||'Sin tipo')}</span><small>${p.latitud!=null&&p.longitud!=null?'📍 Ubicada':'📍 Sin ubicación'}</small></button>`).join('')}</div>${!list.length?'<div class="empty">No hay empresas que coincidan.</div>':''}<div class="section"><button class="section-title" data-section="placeSummary">📊 Resumen <span>▼</span></button><div class="section-body" id="placeSummary"><div class="stats"><span class="stat">${all.length} empresas activas</span><span class="stat">${all.filter(p=>p.latitud!=null&&p.longitud!=null).length} geolocalizadas</span></div></div></div><div class="modal" id="modal"></div>`);}
function renderProduct(){const p=productById(selectedProductId);if(!p){selectedProductId=null;return render();}const pres=presentationsForProduct(p.id);shell(`<div class="app-head"><button class="btn secondary small" id="back">← Productos</button><div class="app-actions"><button class="btn small" id="editProduct">✏️ Editar</button><button class="btn secondary small" id="deactivateProduct">⏸️ Desactivar</button><button class="btn danger small" id="deleteProduct">🗑️ Eliminar</button></div></div><div class="product-detail"><div class="detail-top"><div><h1 class="detail-title">${esc(p.nombre)}</h1><div class="muted">Producto</div></div></div><div class="section-actions"><button class="btn" id="newPresentation">+ Presentación</button></div><div class="presentation-stack">${pres.map(renderPresentation).join('')||'<div class="empty">Este producto aún no tiene presentaciones.</div>'}</div></div><div class="modal" id="modal"></div>`);}
function renderPresentation(pr){const rows=pricesForPresentation(pr.id);const activePlaces=[...new Set(rows.map(r=>r.clienteId))].map(id=>placeById(id)).filter(p=>p&&p.estatus!=='inactivo');const data=activePlaces.map(place=>{const last=latestPriceForPresentationPlace(pr.id,place.id);return {place,last};}).filter(x=>x.last);const low=data.reduce((best,x)=>!best||Number(x.last.precio)<Number(best.last.precio)?x:best,null);return `<div class="presentation-card"><div class="presentation-head"><div><b>📦 ${esc(pr.nombre)}</b>${pr.unidad?` <span class="muted">(${esc(pr.unidad)})</span>`:''}</div><div class="inline-actions"><button class="icon-btn" data-edit-presentation="${pr.id}" title="Editar">✏️</button><button class="icon-btn" data-deactivate-presentation="${pr.id}" title="Desactivar">⏸️</button><button class="icon-btn danger-text" data-delete-presentation="${pr.id}" title="Eliminar">🗑️</button></div></div>${low?`<div class="low-price">💰 Precio más bajo: <b>$${Number(low.last.precio).toFixed(2)}</b> · ${esc(low.place.nombre)} ${ageDays(low.last.createdAt)>30?'<span class="status-bad">🔴 +30 días</span>':'<span class="status-good">🟢 reciente</span>'}</div>`:'<div class="hint">Sin precios registrados.</div>'}<div class="company-list">${data.map(({place,last})=>`<button class="company-row" data-place-id="${place.id}"><span><b>${esc(place.nombre)}</b><small>${dateFmt(last.createdAt)} · ${ageDays(last.createdAt)>30?'🔴 antiguo':'🟢 reciente'}</small></span><strong>$${Number(last.precio).toFixed(2)}</strong></button>`).join('')||'<div class="empty compact">No hay empresas con precio para esta presentación.</div>'}</div><div class="presentation-actions"><button class="btn small" data-price="${pr.id}">+ Precio</button></div></div>`;}
function renderPlace(){const p=placeById(selectedPlaceId);if(!p){selectedPlaceId=null;return render();}const obs=getObservations().filter(o=>o.clienteId===p.id).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt));shell(`<div class="app-head"><button class="btn secondary small" id="backPlace">← Regresar</button><div class="app-actions"><button class="btn small" id="editPlace">✏️ Editar</button><button class="btn secondary small" id="deactivatePlace">⏸️ Desactivar</button><button class="btn danger small" id="deletePlace">🗑️ Eliminar</button></div></div><div class="product-detail"><h1 class="detail-title">🏪 ${esc(p.nombre)}</h1><div class="stats"><span class="stat">${esc(p.tipo)}</span>${p.contacto?`<span class="stat">☎️ ${esc(p.contacto)}</span>`:''}</div><div class="row-card"><b>Información</b><div class="hint">${esc(p.direccion||'Sin dirección registrada')}</div>${p.tiendaVirtual?`<div class="hint">🌐 <a href="${esc(p.tiendaVirtual)}" target="_blank" rel="noopener">Tienda virtual</a></div>`:''}</div><div class="section"><button class="section-title" data-section="placePrices">💰 Precios <span>▼</span></button><div class="section-body list" id="placePrices">${obs.map(o=>`<div class="row-card"><div class="row-main"><div><b>${esc(o.producto)}</b><div class="muted">${esc(o.presentacion||'Sin presentación')} · ${dateFmt(o.createdAt)}</div></div><span class="price">$${Number(o.precio||0).toFixed(2)}</span></div><button class="text-danger" data-delete-observation="${o.id}">🗑️ Eliminar precio</button></div>`).join('')||'<div class="empty">No hay precios registrados.</div>'}</div></div><div class="section"><button class="section-title" data-section="placePhotos">📷 Fotos <span>▼</span></button><div class="section-body"><div id="placePhotoGrid" class="photo-grid"></div><div id="placePhotoEmpty" class="empty">Cargando fotos…</div></div></div></div><div class="modal" id="modal"></div>`);loadPlacePhotos(obs);}
async function loadPlacePhotos(obs){const grid=document.getElementById('placePhotoGrid');const empty=document.getElementById('placePhotoEmpty');const ids=[];obs.forEach(o=>(o.photoIds||[]).forEach(id=>ids.push({id,obsId:o.id})));for(const item of ids){const p=await getPhoto(item.id);if(!p)continue;const url=URL.createObjectURL(p.blob);grid.insertAdjacentHTML('beforeend',`<div class="photo-card"><img src="${url}" alt="${esc(p.name)}"><div><b>${esc(p.placeName)}</b><span class="hint">${esc(p.name)}</span><button class="text-danger" data-delete-photo="${p.id}">🗑️ Eliminar</button></div></div>`);}if(grid.children.length)empty.style.display='none';else empty.textContent='No hay fotos.';bind();}
function openModal(html){const m=document.getElementById('modal');m.className='modal open';m.innerHTML=`<div class="modal-box">${html}</div>`;return m;}
function closeModal(){const m=document.getElementById('modal');if(m)m.className='modal';}
function openProductForm(p=null){const m=openModal(`<h2>${p?'✏️ Editar producto':'➕ Nuevo producto'}</h2><div class="field"><label>Nombre *</label><input id="fName" value="${esc(p?.nombre||'')}"></div><div class="field"><label>Categoría</label><input id="fCat" value="${esc(p?.categoria||'')}" placeholder="Frutas, insumos, etc."></div><div class="modal-actions"><button class="btn secondary" id="cancel">Cancelar</button><button class="btn" id="save">Guardar</button></div>`);m.querySelector('#cancel').onclick=closeModal;m.querySelector('#save').onclick=()=>{try{if(p)updateProduct(p.id,{nombre:m.querySelector('#fName').value,categoria:m.querySelector('#fCat').value});else createProduct({nombre:m.querySelector('#fName').value,categoria:m.querySelector('#fCat').value});closeModal();render();}catch(e){alert(e.message);}};}
function openPresentationForm(pr){const m=openModal(`<h2>${pr?'✏️ Editar presentación':'➕ Nueva presentación'}</h2><div class="field"><label>Nombre *</label><input id="fName" value="${esc(pr?.nombre||'')}"></div><div class="field"><label>Unidad</label><input id="fUnit" value="${esc(pr?.unidad||'')}" placeholder="kg, pieza, caja..."></div><div class="modal-actions"><button class="btn secondary" id="cancel">Cancelar</button><button class="btn" id="save">Guardar</button></div>`);m.querySelector('#cancel').onclick=closeModal;m.querySelector('#save').onclick=()=>{try{if(pr)updatePresentation(pr.id,{nombre:m.querySelector('#fName').value,unidad:m.querySelector('#fUnit').value});else createPresentation({productId:selectedProductId,nombre:m.querySelector('#fName').value,unidad:m.querySelector('#fUnit').value});closeModal();renderProduct();}catch(e){alert(e.message);}};}
function openPlaceForm(p=null){const m=openModal(`<h2>${p?'✏️ Editar empresa':'🏪 Nueva empresa'}</h2><div class="form-grid"><div class="field full"><label>Nombre *</label><input id="fName" value="${esc(p?.nombre||'')}" autofocus></div><div class="field"><label>Contacto *</label><input id="fContact" value="${esc(p?.contacto||'')}"></div><div class="field"><label>Tipo *</label><select id="fType">${['Tienda','Mercado','Abarrotes','Abastos','Proveedor','Otro'].map(x=>`<option ${normalize(x)===normalize(p?.tipo||'Tienda')?'selected':''}>${x}</option>`).join('')}</select></div><div class="field full"><label>Dirección</label><input id="fAddress" value="${esc(p?.direccion||'')}" placeholder="Opcional si usarás ubicación"></div><div class="field full"><label>🌐 Tienda virtual</label><input id="fOnline" type="url" value="${esc(p?.tiendaVirtual||'')}" placeholder="https://..." inputmode="url"></div></div><div class="location-box"><div><b>📍 Ubicación</b><div class="hint" id="geoHint">${p?.latitud!=null&&p?.longitud!=null?'Ubicación guardada.':'Sin ubicación guardada.'}</div></div><button class="btn secondary small" id="useLocation">📍 Usar ubicación actual</button></div><input type="hidden" id="lat" value="${p?.latitud??''}"><input type="hidden" id="lng" value="${p?.longitud??''}"><div class="modal-actions"><button class="btn secondary" id="cancel">Cancelar</button><button class="btn" id="save">Guardar</button></div>`);m.querySelector('#cancel').onclick=closeModal;m.querySelector('#useLocation').onclick=()=>{if(!navigator.geolocation)return alert('Este dispositivo no permite geolocalización.');navigator.geolocation.getCurrentPosition(pos=>{m.querySelector('#lat').value=pos.coords.latitude;m.querySelector('#lng').value=pos.coords.longitude;m.querySelector('#geoHint').textContent=`✓ ${pos.coords.latitude.toFixed(5)}, ${pos.coords.longitude.toFixed(5)}`;},()=>alert('No se pudo obtener la ubicación. Revisa el permiso de ubicación.'));};m.querySelector('#save').onclick=()=>{const name=m.querySelector('#fName').value.trim(),contact=m.querySelector('#fContact').value.trim();if(!name||!contact)return alert('Nombre y contacto son obligatorios.');upsertPlace({id:p?.id,nombre:name,contacto:contact,telefono:contact,tipo:m.querySelector('#fType').value,direccion:m.querySelector('#fAddress').value,tiendaVirtual:m.querySelector('#fOnline').value.trim(),latitud:m.querySelector('#lat').value?Number(m.querySelector('#lat').value):null,longitud:m.querySelector('#lng').value?Number(m.querySelector('#lng').value):null});closeModal();p?renderPlace():render();};}
function confirmDelete(label,fn){const m=openModal(`<h2>⚠️ Eliminar definitivamente</h2><p>Esta acción eliminará <b>${esc(label)}</b>. Para continuar escribe <b>CONFIRMAR</b>.</p><input id="confirmText" autocomplete="off" placeholder="Escribe CONFIRMAR"><div class="modal-actions"><button class="btn secondary" id="cancel">Cancelar</button><button class="btn danger" id="go" disabled>Eliminar</button></div>`);const input=m.querySelector('#confirmText'),go=m.querySelector('#go');input.oninput=()=>go.disabled=input.value.trim()!=='CONFIRMAR';m.querySelector('#cancel').onclick=closeModal;go.onclick=()=>{fn();closeModal();};}
function openPrice(pres){const m=openModal(`<h2>💰 Registrar precio</h2><div class="hint">${esc(productById(selectedProductId)?.nombre)} · ${esc(presentationByIdSafe(pres)?.nombre||'')}</div><div class="field"><label>Empresa *</label><input id="placeSearch" class="search" placeholder="🔎 Buscar empresa..."><div id="placeOptions" class="picker-list"></div></div><input type="hidden" id="place"><div class="field"><label>Precio *</label><input id="price" type="number" min="0" step="0.01" inputmode="decimal"></div><div class="field"><label>Foto</label><input id="photo" type="file" accept="image/*" capture="environment"><div class="hint">La foto se comprime automáticamente.</div></div><div class="modal-actions"><button class="btn secondary" id="cancel">Cancelar</button><button class="btn" id="save">Guardar</button></div>`);const activePlaces=getPlaces().filter(p=>p.estatus!=='inactivo');const renderOptions=()=>{const q=normalize(m.querySelector('#placeSearch').value);const rows=activePlaces.filter(p=>!q||normalize(p.nombre).includes(q)||normalize(p.tipo).includes(q));m.querySelector('#placeOptions').innerHTML=rows.map(p=>`<button type="button" class="picker-item" data-pick-place="${p.id}"><b>${esc(p.nombre)}</b><small>${esc(p.tipo)}</small></button>`).join('')||'<div class="hint">No hay empresas.</div>';m.querySelectorAll('[data-pick-place]').forEach(b=>b.onclick=()=>{m.querySelector('#place').value=b.dataset.pickPlace;m.querySelector('#placeSearch').value=placeById(b.dataset.pickPlace)?.nombre||'';m.querySelectorAll('[data-pick-place]').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');});};m.querySelector('#placeSearch').oninput=renderOptions;renderOptions();let photoIds=[];m.querySelector('#photo').onchange=async e=>{const f=e.target.files?.[0],place=placeById(m.querySelector('#place').value);if(!f||!place)return;photoIds=[await savePhoto(f,place.nombre)];};m.querySelector('#cancel').onclick=closeModal;m.querySelector('#save').onclick=()=>{const place=placeById(m.querySelector('#place').value),price=Number(m.querySelector('#price').value),pr=presentationByIdSafe(pres);if(!place||!(price>=0)||!pr)return alert('Completa empresa y precio.');addObservation({producto:productById(selectedProductId).nombre,presentacion:pr.nombre,presentationId:pr.id,precio:price,clienteId:place.id,photoIds});closeModal();renderProduct();};}
function presentationByIdSafe(id){return getPresentations().find(p=>p.id===id)||null;}
function bind(){document.getElementById('q')?.addEventListener('input',e=>{query=e.target.value;const caret=e.target.selectionStart;render();const q=document.getElementById('q');if(q){q.focus();q.setSelectionRange(caret,caret);}});document.getElementById('filterBtn')?.addEventListener('click',()=>document.getElementById('filters').classList.toggle('open'));document.querySelectorAll('[data-type]').forEach(b=>b.onclick=()=>{typeFilter=b.dataset.type;render();});document.querySelectorAll('[data-product-id]').forEach(b=>b.onclick=()=>{selectedProductId=b.dataset.productId;render();});document.getElementById('newProduct')?.addEventListener('click',()=>openProductForm());document.getElementById('newPlace')?.addEventListener('click',()=>openPlaceForm());document.getElementById('placesView')?.addEventListener('click',()=>{showPlaces=true;placeQuery='';render();});document.getElementById('backPlaces')?.addEventListener('click',()=>{showPlaces=false;render();});document.getElementById('newPlaceFromList')?.addEventListener('click',()=>openPlaceForm());document.getElementById('placeQ')?.addEventListener('input',e=>{placeQuery=e.target.value;const caret=e.target.selectionStart;renderPlaces();const q=document.getElementById('placeQ');if(q){q.focus();q.setSelectionRange(caret,caret);}});document.getElementById('back')?.addEventListener('click',()=>{selectedProductId=null;render();});document.getElementById('backPlace')?.addEventListener('click',()=>{selectedPlaceId=null;render();});document.getElementById('editProduct')?.addEventListener('click',()=>openProductForm(productById(selectedProductId)));document.getElementById('deactivateProduct')?.addEventListener('click',()=>{if(confirm('¿Desactivar este producto? Se conservará su historial.')){deactivateProduct(selectedProductId);selectedProductId=null;render();}});document.getElementById('deleteProduct')?.addEventListener('click',()=>confirmDelete(productById(selectedProductId)?.nombre,()=>{deleteProduct(selectedProductId);selectedProductId=null;render();}));document.getElementById('newPresentation')?.addEventListener('click',()=>openPresentationForm());document.querySelectorAll('[data-edit-presentation]').forEach(b=>b.onclick=()=>openPresentationForm(presentationByIdSafe(b.dataset.editPresentation)));document.querySelectorAll('[data-deactivate-presentation]').forEach(b=>b.onclick=()=>{if(confirm('¿Desactivar esta presentación? Se conservará su historial.')){deactivatePresentation(b.dataset.deactivatePresentation);renderProduct();}});document.querySelectorAll('[data-delete-presentation]').forEach(b=>b.onclick=()=>confirmDelete('esta presentación',()=>{deletePresentation(b.dataset.deletePresentation);renderProduct();}));document.querySelectorAll('[data-place-id]').forEach(b=>b.onclick=()=>{selectedPlaceId=b.dataset.placeId;render();});document.querySelectorAll('[data-price]').forEach(b=>b.onclick=()=>openPrice(b.dataset.price));document.getElementById('editPlace')?.addEventListener('click',()=>openPlaceForm(placeById(selectedPlaceId)));document.getElementById('deactivatePlace')?.addEventListener('click',()=>{if(confirm('¿Desactivar esta empresa? Se conservará su historial.')){deactivatePlace(selectedPlaceId);selectedPlaceId=null;render();}});document.getElementById('deletePlace')?.addEventListener('click',()=>confirmDelete(placeById(selectedPlaceId)?.nombre,()=>{deletePlace(selectedPlaceId);selectedPlaceId=null;render();}));document.querySelectorAll('[data-delete-observation]').forEach(b=>b.onclick=()=>{if(confirm('¿Eliminar este precio?')){deleteObservation(b.dataset.deleteObservation);renderPlace();}});document.querySelectorAll('[data-delete-photo]').forEach(b=>b.onclick=async()=>{if(!confirm('¿Eliminar esta foto?'))return;await deletePhoto(b.dataset.deletePhoto);renderPlace();});document.querySelectorAll('[data-section]').forEach(b=>b.onclick=()=>{document.getElementById(b.dataset.section)?.parentElement.classList.toggle('closed');});}
render();
